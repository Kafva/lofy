import { createSignal, splitProps } from 'solid-js';


// The `Tracks` component will fetch Tracks from the server
// based on a provided `MediaList`

const Tracks = (props: {mediaList: MediaList}) => {
  
  return (<>
  </>);

};

export default Tracks

